In this directory I have the main program which in PHP and html.
It is a dropdown menu with the list of every country.
When a country is selected it activates a PHP script which prepares the data for the specific country an awk script is used to prepare the data and a plot which will be shown in an iframe.  